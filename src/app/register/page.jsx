import RegisterPage from '@/components/auth/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterPage/>
        </div>
    );
};

export default page;